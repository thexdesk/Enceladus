:host {
  contain: content;

  display: inherit;
  grid-template-columns: inherit;
  gap: inherit;
  grid-column: 1 / -1;
}

.tnum {
  font-feature-settings: 'tnum';
}

p {
  line-height: 1.3;
  margin: 0;

  &:not(:first-of-type) {
    margin-block-start: 0.6rem;
  }
}
